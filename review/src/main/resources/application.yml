server:
  port: 8086
  servlet:
    context-path: /review
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
spring:
  application:
    name: limmaReview
  datasource:
    url: jdbc:postgresql://localhost:5430/sapa
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
